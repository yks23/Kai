# 完善指令

你之前已经完成了任务（任务文件已被你删除），但最低执行时间尚未达到。

## 当前状态
- 已用时间: {elapsed_sec:.0f}s / 要求最低: {min_time}s（还需约 {remaining_sec:.0f}s）
- 报告目录: {report_dir}

## 要求

利用剩余时间，针对**你刚才完成的工作**进行精细化完善：

1. **复查**：重新审视你刚才的改动，检查是否有遗漏、逻辑错误或边界情况
2. **测试**：运行相关测试确保改动正确，修复发现的问题
3. **代码质量**：改善命名、消除重复、补充必要的注释
4. **更新报告**：如有改进，修改 `{report_dir}` 中的报告文件

不要为了凑时间做无意义的改动。如果确认没有可改进之处，可以直接停止。
