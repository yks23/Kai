# 续轮指令

你正在处理一个任务，这是本会话的第 N 轮调用（系统通过 --resume 恢复了你之前的完整对话记忆）。

## 当前状态
- 任务文件: `{task_file}`（仍然存在，说明任务尚未完成）
- 报告目录: `{report_dir}`

## 要求

1. **回顾上一轮的工作**，确认进展和当前位置
2. **继续推进任务**，从上次停下的地方继续
3. 如果发现之前有遗漏或错误，先修正再推进
4. **如果任务已全部完成**：
   - 删除任务文件: `rm {task_file}`
   - 在 `{report_dir}` 写完成报告
