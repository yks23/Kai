# 续轮指令

你正在处理一个任务，系统通过 --resume 恢复了你之前的完整对话记忆。

## 当前状态
- 任务文件: `{task_file}`
- 报告目录: `{report_dir}`
{status_section}
## 要求

1. 回顾上一轮的工作，确认进展
2. 继续推进，从上次停下的地方继续；如有遗漏先修正
3. 完成后：删除任务文件 `rm {task_file}`，在 `{report_dir}` 写报告
