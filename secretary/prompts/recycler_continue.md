# 续轮指令

你正在审查一份完成报告，系统通过 --resume 恢复了你之前的对话记忆。

## 当前状态
- 报告文件: {report_file}
- 已解决目录: {solved_dir}
- 未解决目录: {unsolved_dir}

## 要求

1. 回顾上一轮的审查结果
2. 如果判定未完成，继续执行移动操作
3. 将报告移到 `{solved_dir}` 或 `{unsolved_dir}`
